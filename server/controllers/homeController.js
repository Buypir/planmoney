// Контролер: логіка для головної сторінки
// Тут описуємо, ЩО робити, коли хтось звертається на "/"

const getHome = (req, res) => {
  res.send('Привіт! Це сервер PlanMoney 🚀');
};

// Експортуємо функцію, щоб її можна було використати в інших файлах
module.exports = { getHome };